name: blutooth_serial_sender
description: Text commands sender

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: 7.2.0
  provider: 6.0.0
  shared_preferences: ^2.0.12
  flutter_bluetooth_serial: ^0.4.0
  cupertino_icons: ^1.0.2
  scoped_model: ^2.0.0-nullsafety.0
  rive: 0.7.32
  noise_meter: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  assets:
    - assets/images/logo.jpeg

  uses-material-design: true
